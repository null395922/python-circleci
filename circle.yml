dependencies:
  override:
    - pip install -U pytest
    - git clone https://github.com/poldrack/python-circleci.git
test:
  override:
    - cd python-circleci
    - find . -name \*.pyc -o -name \*.pyo -o -name __pycache__ -exec rm -rf {} +
    - pytest
